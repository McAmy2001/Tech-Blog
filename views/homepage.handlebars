<ol class="post-list">
  {{#each posts as |post|}}
  <li>
    <article class="post">
      <div class="title">
        <a href="{{post.title}}" target="_blank">{{post.title}}</a>
        <p>{{post.blog_text}}</p>
      </div>
      <div class="meta">
        <a href="/post/{{post.idid}}">{{post.comments.length}} comment(s)</a>
      </div>
    </article>
  </li>
  {{/each}}
</ol>